rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Solo usuarios autenticados pueden acceder a cualquier cosa
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
